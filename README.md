# FixPoint

## A place to manage your properties simple

### Start in localhost.
1. Clone repo
2. Install the dependencies `npm install`
3. Create your environment variables following the file example `.env.example``
4. Run with `npm run dev`
5. Open the app in http://localhost:5173/